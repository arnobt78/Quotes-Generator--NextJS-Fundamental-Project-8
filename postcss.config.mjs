/** @type {import('postcss-load-config').Config} */
/** PostCSS: Tailwind CSS plugin for processing utility classes */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
