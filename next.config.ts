import type { NextConfig } from "next";

/** Next.js config. Empty defaults work for this project; add rewrites/images here if needed. */
const nextConfig: NextConfig = {};

export default nextConfig;
